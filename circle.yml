general:
  branches:
    ignore:
      - gh-pages

machine:
  timezone: Asia/Tokyo
  node:
    version: v6.10

dependencies:
  override:
    - npm install

test:
  override:
    - "true"

deployment:
  deploy:
    branch: master
    commands:
      - ./scripts/deploy.sh
